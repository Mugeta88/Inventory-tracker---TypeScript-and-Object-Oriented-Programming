class Product {
    sku: string;
    name: string;
    price: string;


    constructor(sku: string, name: string, price: string){
        this.sku = sku;
        this.name = name;
        this.price = price;
    }

    displayDetails(): string {
        return ``
    }
}